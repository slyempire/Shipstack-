[project]
name = "shipstack"
authors = [
    { name = "Shipstack", email = "info@shipstack.io" }
]
description = "Shipstack logistics platform integration for Frappe"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "shipstack"
